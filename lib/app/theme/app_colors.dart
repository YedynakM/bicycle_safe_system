import 'package:flutter/material.dart';

class AppColors {
  // Основні кольори
  static const Color primary = Color(0xFF00E5FF); // Неоновий блакитний
  static const Color accent = Color(0xFFFF4081);  // Рожевий акцент (для 2-ї шкали)
  
  // Фон
  static const Color backgroundDark = Color(0xFF1E1E1E);
  static const Color cardSurface = Color(0xFF2C2C2C);
  
  // Текст
  static const Color textMain = Colors.white;
  static const Color textDim = Colors.grey;

  // Індикатори
  static const Color success = Colors.greenAccent;
  static const Color error = Colors.redAccent;
  static const Color transparentBlack = Colors.black54;
}